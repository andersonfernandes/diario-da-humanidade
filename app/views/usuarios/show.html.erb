<div class="container">
  <div class="row">
    <div class="col-md-12">
      <div class="card">
	      <div class="content">
	      	<h3 class="text-center"><%= @usuario.name %></h3>

	      	<div class="row">
						<% @posts.each do |post| %>
							<div class="col-md-4 text-center">
								<%= render partial: 'posts/post_card',
												 locals: { post: post } %>
							</div>
						<% end %>
					</div>
	      </div>
      </div>
  	</div>
  </div>
</div>