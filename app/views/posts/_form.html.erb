<%= simple_form_for @post do |f| %>
  <div class="row">
    <div class="col-md-6">
      <%= f.input :titulo, required: true %>
    </div>

    <div class="col-md-6 text-center">
      <%= f.input :anonimo, label: 'Post Anônimo?' %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <%= f.input :texto, required: true, label: false, input_html: { class: 'summernote' } %>
    </div>
  </div>

  <%= f.button :submit, "Criar", class: 'btn-raised btn-success pull-right',
                data: { disable_with: 'Criando...' } %>
  <%= link_to 'Cancelar', :back, class: 'btn btn-danger btn-link pull-right' %>
<% end %>
